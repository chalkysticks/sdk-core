
interface IGoogleAnalytics {
    id: string;
}

interface IEnvironmentApplication {
    api_url: string;
    limit: number;
}

interface IEnvironmentGoogle {
    analytics: any;
    api_key: string;
}
